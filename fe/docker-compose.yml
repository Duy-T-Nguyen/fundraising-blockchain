services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fundraising-frontend
    expose:
      - "80"
    networks:
      - fundraising-network
    restart: unless-stopped

networks:
  fundraising-network:
    external: true
